import React,{useEffect,useRef}from'react';import{SystemStatus}from'../types';interface Props{status:SystemStatus};const NeuralVisualizer:React.FC<Props>=({status})=>{const canvasRef=useRef<HTMLCanvasElement>(null);useEffect(()=>{const canvas=canvasRef.current;if(!canvas)return;const ctx=canvas.getContext('2d');if(!ctx)return;let frame:number;let particles:any[]=[];const init=()=>{canvas.width=window.innerWidth;canvas.height=window.innerHeight;particles=[];for(let i=0;i<100;i++)particles.push({x:Math.random()*canvas.width,y:Math.random()*canvas.height,z:Math.random()*500,vx:(Math.random()-.5)*2,vy:(Math.random()-.5)*2,vz:(Math.random()-.5)*2});};const render=()=>{ctx.clearRect(0,0,canvas.width,canvas.height);particles.forEach(p=>{p.x+=p.vx;p.y+=p.vy;p.z+=p.vz;if(p.x<0||p.x>canvas.width)p.vx*=-1;if(p.y<0||p.y>canvas.height)p.vy*=-1;if(p.z<0||p.z>500)p.vz*=-1;const perspective=500/(500+p.z);ctx.beginPath();ctx.arc(p.x,p.y,2*perspective,0,Math.PI*2);ctx.fillStyle='rgba(255,255,255,.5)';ctx.fill()});frame=requestAnimationFrame(render)};init();render();window.addEventListener('resize',init);return()=>{cancelAnimationFrame(frame);window.removeEventListener('resize',init)}},[status]);return <canvas ref={canvasRef} className="fixed inset-0 pointer-events-none z-0 bg-black"/>};export default NeuralVisualizer;
