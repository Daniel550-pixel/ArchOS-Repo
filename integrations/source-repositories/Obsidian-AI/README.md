# Obsidian-AI source snapshot

Imported into ArchOS as a capability source. Gemini multimodal generation/grounding and Firebase persistence are candidates for ArchOS-native adapters. Credentials and user-specific authorization rules are intentionally not imported into the runtime.
