# Vesper.ai — Operational AI Infrastructure

Single-viewport landing page recreation. The HTML references the exact supplied hero video URL and requires `inter.woff2` and `instrument-serif-italic.woff2` beside `index.html` for self-hosted fonts; Google Fonts fallback is intentionally not fetched by JavaScript.
